import java.util.HashMap;
import java.util.Map;
import java.util.Set;

public class TestDirectedGraph{

    public static void main(String args) {
        
        getVertices();
        getNumberOfVertices();
        getNumberOfEdges();
    }

    public static void getVertices() {
        Map<Character, Set<String>> graf = new HashMap<Character, Set<String>>();
        assert (DirectedGraph.getVertices() ==);
        assert (DirectedGraph.getVertices() ==);
    }

    public static void getNumberOfEdges() {
        Map<Character, Integer> graf = new HashMap<Character, Integer>();
        assert (DirectedGraph.getNumberOfEdges() ==);
        assert (DirectedGraph.getNumberOfEdges() ==);

    }

    public static void getNumberOfVertices() {
        Map<Character, Integer> graf = new HashMap<Character, Integer>();
        assert (DirectedGraph.getNumberOfVertices() ==);
        assert (DirectedGraph.getNumberOfVertices() ==);
    }
}